name: PR Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    permissions:
      contents: read
      pull-requests: write
    uses: challengeit/.github/.github/workflows/claude-pr-review.yml@development
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}